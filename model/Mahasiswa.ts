export class Mahasiswa {
    public npm:string
    public nama:string
    public ipk:string
    
    constructor(npm: string, nama: string, ipk: string) {
        this.npm = npm
        this.nama = nama
        this.ipk = ipk
    }
}